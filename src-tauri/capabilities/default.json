{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window. Deliberately minimal: it grants only Tauri's core defaults. Privileged capabilities are added per feature, never pre-emptively, so the webview's reach stays auditable (ADR-001, ADR-006).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
